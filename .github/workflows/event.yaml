name: Secrets
on: [push]

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: API KEY
        run: echo "API_KEY is ${{ secrets.API_KEY }}"

