name: Manual Workflow
on: 
  workflow_dispatch:
jobs:
  show-info:
    runs-on: ubuntu-latest
    steps:
    - name: Info
      run: |
        echo "This is a push event on all the branches"
        
    
 
